{% extends 'base.html.twig' %}

{% block title %}Hello CategoryController!
{% endblock %}

{% block body %}


	<div class="font-baloo font-bold mx-auto leading-7">
		<h1>Categories ✅</h1>
		{% for category in categories %}
			<a href="{{path('category_show', {categoryName: category.name})}}" class="btn">

				{{category.name}}
			</a>
			<h1>{{category.name}}</h1>
		{% endfor %}
	</div>
{% endblock %}
